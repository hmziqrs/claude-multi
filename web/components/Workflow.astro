---
/**
 * Instance graph (root node + fanned branch chains); SVG-free CSS connectors, no <svg> markup.
 */
import Icon from './Icon.astro';

interface WorkflowNode {
  icon?: string;
  label?: string;
  sublabel?: string;
  status?: string;
  tone?: string;
}

interface WorkflowBranch {
  active?: boolean;
  chain?: WorkflowNode[];
}

interface Props {
  root?: { icon?: string; label?: string; sublabel?: string };
  branches?: WorkflowBranch[];
  animated?: boolean;
}

const { root = {}, branches = [], animated = true } = Astro.props;
const branchList = Array.isArray(branches) ? branches : [];
const hasActive = branchList.some((b) => b && b.active);
---

<div class="ui-workflow">
  <div class="ui-workflow__root">
    <span class="ui-workflow__root-icon">
      <Icon name={root.icon || 'layers'} size={13} />
    </span>
    <div class="ui-workflow__root-text">
      <span class="ui-workflow__root-label">{root.label}</span>
      {root.sublabel && <span class="ui-workflow__root-sub">{root.sublabel}</span>}
    </div>
  </div>

  <div class="ui-workflow__branches">
    <div
      class="ui-workflow__rail"
      aria-hidden="true"
      style={`background:${hasActive ? 'var(--border)' : 'var(--border-subtle)'}`}
    ></div>
    {
      branchList.map((branch) => {
        const chain = branch && Array.isArray(branch.chain) ? branch.chain : [];
        const isActive = !!(branch && branch.active);
        const flow = isActive && animated;
        const solidColor = isActive ? 'var(--border)' : 'var(--border-subtle)';
        const lineStyle = flow ? undefined : `background:${solidColor};`;
        return (
          <div class="ui-workflow__branch">
            <div class="ui-workflow__elbow" aria-hidden="true">
              <div
                class:list={['ui-workflow__elbow-line', { 'ui-workflow__elbow-line--flow': flow }]}
                style={lineStyle}
              />
            </div>
            <div class="ui-workflow__chain">
              {chain.map((node, j) => (
                <Fragment>
                  <div class:list={['ui-workflow__node', { 'ui-workflow__node--bright': isActive }]}>
                    <div class="ui-workflow__node-top">
                      {node.icon && <Icon name={node.icon} size={12} />}
                      <span class="ui-workflow__node-label">{node.label}</span>
                      {node.status === 'running' && (
                        <span class="ui-workflow__dot" title="running" aria-hidden="true" />
                      )}
                    </div>
                    {node.sublabel && (
                      <span
                        class:list={['ui-workflow__node-sub', { 'ui-workflow__node-sub--violet': node.tone === 'violet' }]}
                      >
                        {node.sublabel}
                      </span>
                    )}
                  </div>
                  {j < chain.length - 1 && (
                    <div class="ui-workflow__connector" aria-hidden="true">
                      <div
                        class:list={['ui-workflow__connector-line', { 'ui-workflow__connector-line--flow': flow }]}
                        style={lineStyle}
                      />
                    </div>
                  )}
                </Fragment>
              ))}
            </div>
          </div>
        );
      })
    }
  </div>
</div>

<style>
  .ui-workflow {
    font-family: var(--font-sans);
    color: var(--text-primary);
  }

  .ui-workflow__root {
    display: flex;
    align-items: center;
    gap: 10px;
    background: var(--surface-code);
    border: 1px solid var(--accent-border);
    border-radius: var(--radius-md);
    padding: 10px 12px;
  }
  .ui-workflow__root-icon {
    flex: none;
    width: 26px;
    height: 26px;
    border-radius: var(--radius-sm);
    background: var(--accent-soft);
    border: 1px solid var(--accent-border);
    color: var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .ui-workflow__root-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }
  .ui-workflow__root-label {
    font-size: 12.5px;
    font-weight: 600;
    line-height: 1.3;
  }
  .ui-workflow__root-sub {
    font-family: var(--font-mono);
    font-size: 10.5px;
    line-height: 1.4;
    color: var(--text-faint);
  }

  .ui-workflow__branches {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: 12px;
  }
  .ui-workflow__rail {
    position: absolute;
    left: 0;
    top: -12px;
    bottom: 30px;
    width: 1px;
  }

  .ui-workflow__branch {
    display: flex;
    align-items: stretch;
  }
  .ui-workflow__elbow {
    flex: none;
    width: 16px;
    position: relative;
  }
  .ui-workflow__elbow-line {
    position: absolute;
    top: 26px;
    left: 0;
    right: 0;
    height: 1px;
  }

  .ui-workflow__chain {
    flex: 1 1 auto;
    min-width: 0;
    display: flex;
    align-items: stretch;
  }

  .ui-workflow__node {
    flex: 1 1 0;
    min-width: 0;
    background: var(--surface-code);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-md);
    padding: 10px 12px;
    display: flex;
    flex-direction: column;
    gap: 3px;
  }
  .ui-workflow__node--bright {
    border-color: var(--border);
  }
  .ui-workflow__node-top {
    display: flex;
    align-items: center;
    gap: 7px;
    min-height: 15px;
  }
  .ui-workflow__node-label {
    font-size: 12.5px;
    font-weight: 500;
    line-height: 1.3;
    color: var(--text-primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .ui-workflow__dot {
    flex: none;
    margin-left: auto;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--success);
    animation: ui-wf-pulse 2s ease-in-out infinite;
  }

  .ui-workflow__elbow-line--flow,
  .ui-workflow__connector-line--flow {
    background-color: transparent;
    background-image: repeating-linear-gradient(
      90deg,
      var(--accent-border) 0,
      var(--accent-border) 4px,
      transparent 4px,
      transparent 8px
    );
    animation: ui-wf-dash 1s linear infinite;
  }

  @keyframes ui-wf-dash {
    from { background-position: 0 0; }
    to { background-position: 16px 0; }
  }

  @keyframes ui-wf-pulse {
    0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(74, 222, 128, 0.35); }
    50% { opacity: 0.75; box-shadow: 0 0 0 4px rgba(74, 222, 128, 0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .ui-workflow__elbow-line--flow,
    .ui-workflow__connector-line--flow,
    .ui-workflow__dot {
      animation: none !important;
    }
  }
  .ui-workflow__node-sub {
    font-family: var(--font-mono);
    font-size: 10.5px;
    line-height: 1.4;
    letter-spacing: 0.01em;
    color: var(--text-faint);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .ui-workflow__node-sub--violet {
    color: #a78bfa;
  }

  .ui-workflow__connector {
    flex: none;
    width: 12px;
    display: flex;
    align-items: center;
  }
  .ui-workflow__connector-line {
    width: 100%;
    height: 1px;
  }

  @media (max-width: 640px) {
    .ui-workflow__branch {
      flex-direction: column;
    }
    .ui-workflow__elbow {
      width: auto;
      height: 12px;
    }
    .ui-workflow__elbow-line {
      top: 0;
      bottom: 0;
      left: 7px;
      right: auto;
      width: 1px;
      height: auto;
    }
    .ui-workflow__chain {
      flex-direction: column;
    }
    .ui-workflow__connector {
      width: auto;
      height: 12px;
      align-items: center;
      justify-content: center;
    }
    .ui-workflow__connector-line {
      width: 1px;
      height: 100%;
    }
  }
</style>
